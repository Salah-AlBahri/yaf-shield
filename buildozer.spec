[app]
title = Expert Shield
package.name = yafshield
package.domain = org.salah
source.dir = .
source.include_exts = py,png,jpg,kv,atlas
version = 0.1
requirements = python3,kivy,requests,urllib3,certifi,chardet,idna

# إعدادات الخلفية والأيقونة التي ربطناها بالصورة الجديدة
icon.filename = background.png
presplash.filename = background.png

orientation = portrait
fullscreen = 0
android.archs = armeabi-v7a
